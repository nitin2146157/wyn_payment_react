import React from "react";

const ExpireCron = () => {
  return (
    <div className="flex flex-col items-center justify-center min-h-screen bg-gray-100">
      <div className="text-6xl font-bold text-gray-800">200</div>
      <div className="mt-4 text-center">
        <h3 className="text-2xl font-semibold text-gray-700">Expire Cron</h3>
        <p className="text-lg text-gray-600 mt-2">You have run the expire cron.</p>
      </div>
    </div>
  );
};

export default ExpireCron;
